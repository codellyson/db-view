// Stub. The real query editor lives in apps/next/app/components/query-editor.tsx.
export function Query() {
  return (
    <main className="container mx-auto px-6 sm:px-8 py-16">
      <div className="max-w-2xl mx-auto">
        <h1 className="text-3xl font-semibold text-primary mb-2">Query</h1>
        <p className="text-sm text-muted">
          Vite port in progress. Codemirror + the editability analyzer port to come.
        </p>
      </div>
    </main>
  );
}
